# FlappyBird
Easy game in python.  

## Usage
To run it, you need to install:
```bash
cd flappybird
python3 -m venv myenv
source myenv/bin/activate
```
(myenv) user@pc myenv %
```bash
pip install pygame
```